# Component Gallery

## 核心结论
- 这个 episode 用来逐个展示当前注册表里的组件。
- `SectionStamp` 是 ready 组件，其余高级组件仍是 prototype 或 planned 占位。
- 它用于视觉调试，不作为正式成片模板。

## 口播原稿
这是组件展示用 demo。每个镜头只展示一种主组件，方便在 Remotion Studio 中逐段调整布局、slot 和视觉方向。

## 本期备注
- 不使用外部素材。
- 不伪造新闻、官网、数据或截图。
- planned 组件不能用于正式高清渲染。
